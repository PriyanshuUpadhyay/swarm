# Council: next swarm task (2026-09-15)

Artifact: repository <repo> at 4c79c29; outline
<outline>; task files
~/.claude/pair/2026-09-15-swarm-{close-drain,readme-cleanup,council-demo}.md.

Question: which task next, the heartbeat sweeper loop (`swarm sweep --every N`), R8
temp-then-rename message bodies, R21 opaque random session ids, or the small demo follow-ups?

Models: GEMINI gemini-3.8-flash-high (agy, high); GPT gpt-6-astra (codex, high); CLAUDE
claude-fable-5-1 (xhigh). Backend Herdr, visible panes w8A:p7/p8/p9. Chair: this session.

## Round 0

All three: NO QUESTIONS. Wall time 14m27s (Gemini slow; others under 3m).

## Council verdict: GO (2-1 on label; unanimous on the task)

Consensus reasoning: The sweeper loop is the last decided S5 piece not built (outline lines
177-178, 351). Today `sweep` is one pass (src/main.rs:193-203) and the demo calls it by hand
(demo/council.sh:49). An orchestrator ends its turn and waits to be woken (R16), so a killed
pane is reported to nobody until the loop exists. Every piece it needs already exists
(report_dead src/main.rs:64-77, has_pane src/adapter.rs:59-63), so it is one pair task with no
schema change. R8 is unanimous second choice; R21 needs a migration and is more than one task.

Required changes (accepted by all three):
1. Plain `swarm sweep` unchanged; the pass is extracted (implementation chunk, not a behavior).
2. `--every <secs>` positive integer; bad value prints USAGE (src/main.rs:21) and exits 1;
   README.md:40 table row updated.
3. Loop: pass, sleep N, repeat; `dead <id>` per pass; a second pass prints nothing for the
   same child (has_summary + pane_id IS NOT NULL make it idempotent).
4. In-tick adapter error: warn to stderr and continue. Setup errors before the first tick exit 1.
5. demo/council.sh: start `sweep --every 1 &` with an EXIT trap before the ghost spawn, drop
   the manual call, wait on the inbox for the ghost fallback; still exits 0.

Dissent / residual risk: store (SQLite) error inside a tick, three-way split, user tie-break:
- CLAUDE WARN-CONTINUE: a partial tick loses only a summarize job that cannot succeed without
  a log (no exited hook ran); next tick is safe to repeat; likely error is a busy timeout.
- GPT STOP-ON-STORE-ERROR: summary can commit then enqueue fail; next tick skips the job
  because has_summary is true; stop and report rather than hide the loss.
- GEMINI STOP-AFTER-3 consecutive failed ticks, mirroring drain's attempts < 3 (Claude offered
  then withdrew this: a write burst of busy timeouts would stop the guard for good).

Per-model trail:
- GEMINI: r1 sweeper loop GO, 5 behaviors; r2 SKIP-AND-WARN, 6 behaviors incl. demo; r3 DEMO-LOOP,
  STOP-AFTER-3, GO.
- GPT: r1 sweeper loop GO, 3 behaviors, adapter errors stop loop; r2 SKIP-AND-WARN adapter /
  STOP store, 5 behaviors, no demo change; r3 DEMO-LOOP conceded, STOP-ON-STORE-ERROR,
  GO-WITH-CHANGES.
- CLAUDE: r1 sweeper loop GO, 6 behaviors; r2 SKIP-AND-WARN, 5 behaviors, demo kept; r3
  DEMO-LOOP, WARN-CONTINUE, GO.

Converged on the task at round 1, on adapter policy and demo at rounds 2-3; store-error policy
left to the user after round 3. Wall times: r0 14m27s, r1 5m29s, r2 3m52s, r3 4m07s.
Scratch: /tmp/councils/36e53f9a-bedd-42df-a6fb-ed28b68795d1 (removed after this log).
